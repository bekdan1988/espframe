# =============================================================================
# DEV CONFIG - Local development entry point (Guition ESP32-P4 JC4880P443)
# =============================================================================
# Flash with: esphome run dev.yaml
# =============================================================================

substitutions:
  name: "immich-frame-43inch-dev"
  friendly_name: "Immich Frame 43inch Dev"
  espframe_component_url: "../.."
  espframe_component_ref: "HEAD"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

packages:
  espframe: !include packages.yaml
