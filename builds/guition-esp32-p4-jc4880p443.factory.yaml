# =============================================================================
# FACTORY FIRMWARE - Guition ESP32-P4 JC4880P443
# =============================================================================
# Secrets-free config for the ESPHome web installer. Boots into AP mode for
# captive portal WiFi setup. This CI build is not used locally.
# Use for rear-case marking 2628+; keep the original build for 2627 or lower.
# =============================================================================

substitutions:
  name: "immich-frame-43inch"
  friendly_name: "Immich Frame 43inch"
  firmware_version: "0.0.0"

packages:
  espframe: !include ../devices/guition-esp32-p4-jc4880p443/packages.yaml

external_components:
  - source:
      type: local
      path: ../components
    components: [gsl3680, remote_image, espframe]

web_server:
  port: 80
  version: 3
  js_url: ""
  css_include: "../docs/public/webserver/style.css"
  js_include: "../docs/public/webserver/app.js"
